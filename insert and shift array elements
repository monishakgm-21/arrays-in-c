#include <stdio.h>
int main()
{
    int size=5;
    int arr[5]={1,3,5,7,9};
    int in=29;
    int pos=3;
   for(int i=size-1; i>=pos-1; i--)
   {
       arr[i+1]=arr[i];
   }
   arr[pos-1]=in;
   size++;
   for(int i=0; i<size; i++)
   {
       printf("%d\n",arr[i]);
   }
   return 0;
}
